import { DateLimiteVentePipe } from './date-limite-vente.pipe';

describe('DateLimiteVentePipe', () => {
  it('create an instance', () => {
    const pipe = new DateLimiteVentePipe();
    expect(pipe).toBeTruthy();
  });
});
