import { test } from '@playwright/test';

// Stock
test('doit afficher une liste d\'objets filtrée sur les statuts \'A vendre\' et \'En vente\'', async ({ page }) => {

});

test('doit filtrer la liste d\'objets', async ({ page }) => {

});

test('doit créer un nouvel objet', async ({ page }) => {

});

test('doit modifier un objet', async ({ page }) => {

});

test('doit mettre en vente un objet', async ({ page }) => {

});

test('doit donner un objet', async ({ page }) => {

});

test('doit supprimer un objet', async ({ page }) => {

});

// Ventes
test('doit afficher une liste de ventes filtrée sur le statuts \'En cours\'', () => {

});

test('doit filtrer la liste de ventes', async ({ page }) => {
  
});

test('doit modifier une vente', async ({ page }) => {

});

test('doit finaliser une vente', async ({ page }) => {

});

test('doit annuler une vente', async ({ page }) => {

});

// Dons
test('doit afficher une liste de dons', async ({ page }) => {

});

test('doit filtrer la liste de dons', async ({ page }) => {
  
});

// Référentiels
test('doit afficher une liste de référentiels', async ({ page }) => {

});

test('doit filtrer la liste de référentiels', async ({ page }) => {
  
});

test('doit modifier un référentiel', async ({ page }) => {

});

test('doit supprimer un référentiel', async ({ page }) => {

});
